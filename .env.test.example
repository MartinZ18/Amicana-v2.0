# Credenciales para tests de integración (actividad-02-api-mock y test_amicana.py).
# Copiar a .env.test (ignorado por git) y completar con valores reales del entorno local.

# ── Pytest (backend) ─────────────────────────────────────────────
TEST_USER_EMAIL=test_amicana_pytest@mail.com
TEST_USER_PASSWORD=admin123
TEST_ADMIN_EMAIL=admin@amicana.com
TEST_ADMIN_PASSWORD=admin123

# ── Playwright (tests de API reales) ─────────────────────────────
# Mismas variables, leídas por los specs de actividad-02-api-mock
